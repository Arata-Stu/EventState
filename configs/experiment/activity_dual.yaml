# @package _global_
# Compare with h_distill_lstm_zloss on the same dsec_det_train41 final fit.
experiment:
  name: activity_dual_lstm

loss:
  kind: dense
  h_distill:
    enabled: true
    cosine_weight: 1.0
    mse_weight: 1.0
  z_objective:
    type: direct_dino
    weight: 1.0
    cosine_weight: 1.0
    mse_weight: 1.0

dataset:
  activity_mask: true

training:
  validation_enabled: false
