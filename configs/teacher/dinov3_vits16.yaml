type: dinov3_vits16
feature: x_norm_patchtokens
embedding_dim: 384
patch_size: 16
frozen: true

# "package" imports an installed `dinov3` package. "torch_hub" supports
# either the official GitHub repository or a separately cloned local copy.
backend: torch_hub
repository: facebookresearch/dinov3:adc254450203739c8149213a7a69d8d905b4fcfa
source: github
checkpoint: null
pretrained: true

cache_features: true
cache_dir: ${oc.env:EVENT_STATE_CACHE,cache}/dinov3_vits16
cache_dtype: float16

image_mean: [0.485, 0.456, 0.406]
image_std: [0.229, 0.224, 0.225]
